resources:
  - kind: folder
    selector:
      query: 'true'
      folders:
        - path: Port-app-config.yml/*
          repos:
            - ios_project
    port:
      entity:
        mappings:
          identifier: .folderName
          blueprint: '"folder"'
          properties:
            link: "'ac'"
